# Supervisor handover

The bounded grinder workflow is established; see `readiness.md`, generated status and validation receipts. The project remains an early hybrid reconstruction, with general linking and most code recovery open.

On the current desktop, use GPT-6 Sol/high for systemic supervisor research and GPT-6 Luna/high for routine or bounded worker investigations. Notebook wall-clock observations are host-specific; compare new desktop timing only within a separately recorded host cohort. The current emphasis is informative compiler feedback and adaptive, evidence-backed batches, with production acceptance unchanged. See `worker-research.md` and the research-only `tools/research_batch.py`.

Next priorities:

Run `python tools/reconstruction_factory.py reclassify` after a capability or mapping change. This refreshes the queue and regenerates the research-only blocker census and build-topology report. Inspect `recovery/blocker-census.json` and `recovery/build-topology.json` before choosing binder, mapping or source work; their categories are overlapping evidence, not automatic eligibility. The current systemic experiment and measured limits are in `recovery/experiments/systemic-capabilities-20260923.md`.

Prioritize complete candidate OMF modes for the direct far-call population. `tools/derive_code_aliases.py` derives far-code target addresses from independent mapped callers; read the current coverage in `recovery/blocker-census.json`. These are binding aliases, not original PUBDEF or TU claims. The pristine MZ relocation table establishes relocated segment words, not OMF target/frame methods. The dated archived OMF mode inventory and its observed limits are in `recovery/experiments/code-address-leverage-20260924.md`. `tools/tu_context.py` freezes a target source while varying ordered snippets and archives whole-object/public/fixup effects. Its local-symbol parser option is research only; production still rejects MSC local symbol records. Use the candidate runs from `build_topology.py` as context hypotheses, never as original TU claims.

`tools/candidate_omf_census.py` now refreshes the current local candidate-object mode inventory during `reclassify`, deduplicating copied archives by object hash. Its mode counts are sampled compiler outputs, not original OMF population estimates. `tools/shared_base_probe.py` then intersects independently mapped public offsets and reviewed private-DATA reference hypotheses for the eligible candidate objects. Its placement result stays separate from complete extent, fixups, historical LINK behavior and native acceptance; see `recovery/experiments/shared-base-checkpoint-20260924.md`. For a single-public direct-flow wrapper with reviewed zero-addend external far CALLs, `tools/prepare_far_call_candidate.py STABLE_ID --source recovery/candidates/NAME.c` freshly checks the complete object, original MZ order, address aliases, bound pristine bytes and CFG before creating a recipe and overlay. Add `--binding external-far-call-dgroup-offset16-v1` only for the historically tested one-far-CALL plus reviewed DGROUP-offset subset. Its CFG review admits direct conditional/unconditional branches and rejects indirect flow; the original OMF/MZ relocation order remains binding authority even if mapped site lists are sorted. Review both outputs, refresh, then use `grind.py attempt` and `grind.py promote`; preparation itself is not acceptance. See `recovery/experiments/far-wrapper-promotions-20260924.md` and `recovery/experiments/mixed-far-data-link-20260924.md`.

`is_facing_camera` remains explicitly blocked. Its true 200-byte pristine extent and bounded far-call capability are proven; the ordinary-C hypothesis still emits 194 bytes. Reopen only for new source-layout or boolean-lowering evidence, not another arbitrary flag sweep. See `python tools/context.py is_facing_camera --history` and `workflow-canary.md` when a new systemic result is relevant.

The desktop declaration-order experiment in `recovery/experiments/is_facing_camera/declaration-order-20260923.md` explained the paired BP-home permutation for one source/profile. Its byte-result cast cross-test emitted 198 bytes and AL/CBW, but kept the wrong early zero-return topology and lacked the trailing false epilogue. It does not reopen the canary.

The independent `recovery/compiler-evidence/boolean-return-placement` fixtures refute a simple trailing-label explanation: pinned MSC 5.1 emits identical complete code for early return versus a trailing zero-return label, retaining AL/CBW but placing the zero path first. This does not reopen the canary or spend a source attempt. Inspect that negative result before trying to move its epilogue with `goto`.

1. Extend only the next required binder mode. External DGROUP offset16 and zero-addend pointer32 far CALLs with target frames now have historical LINK differentials. General near self-relative production, other frames, displacements and segment aliases remain blocked. The local-call fixture proves compiler-emitted PUSH CS + near CALL, but does not enable multi-public production. See `data-binding.md` and `workflow-canary.md`. Do not use Empires' caller-frame shortcut.
   The former routine candidate `sub_35DE6` now has a supervisor interval blocker: its same-CODE-segment `incnums` table requires a CS access path unsupported by the reviewed binding and tested ordinary MSC 5.1 forms. See `recovery/corpus/endurance-001/handoff.md` before proposing new segment/TU evidence.
   The CS-data family audit in `recovery/cs-data-family-20260923.md` identifies three related sprite contributions and one separate interrupt contribution; no common source/binder capability has been proven.
2. Establish original TU/segment contribution relationships from relocated call frames, segment/group/public metadata and multi-function compilation. Restunts segNNN and modern C files are hypotheses only. The rectangle pair has a repeatable 114-byte full-contribution TU probe; remaining TU and segment context is unproven.
3. The three `LIBH` long-arithmetic helpers are now bound (305 bytes); eight historical LINK experiments prove their exact ordered-record semantics. Preserve the default overlap rejection and pinned member/record policies (`library-binding.md`). Next audit the startup COMENT checksum anomaly and extend runtime identification; do not disable checksum checks globally.
4. Support QuickC BAKPAT with format-grounded tests, or continue rejecting it. Compare versions using discriminating real functions. MSC5.0 and5.1 are both exact on current matches; neither the FAQ nor one lucky match proves 5.1 uniquely.
5. Expand fully mapped functions from the current inventory and classify unknown initialized bytes. The 38 imported source code segments are not yet an original TU count. Avoid claiming a total raw-code count until code/data boundaries are complete.

Before turning a long imported interval into a C task, run the read-only `python tools/audit_function_extents.py`. Its single-entry CFG review flags mapped intervals with non-NOP unreachable bytes, and reclassify records them as a distinct supervisor family. In particular, `vector_op_unk` has a RETF at `0x23068` followed by nine bytes beginning another prologue inside its imported extent. Resolve its tail ownership before creating a recipe. The audit makes no automatic boundary or eligibility changes; see `recovery/experiments/empires-method-transfer-20260923.md`.

The 122-byte `rect_union` contribution was promoted through the reviewed DGROUP binder and fresh whole-image acceptance. Its complete pristine CFG, two independently mapped initialized BSS words, conditional-expression source, and research comparison are documented in `rect-union-binding.md`. The 166-byte explicit if/else alternative was a useful negative discriminator.
The adjacent 132-byte `rect_intersect` contribution is also promoted. Its first pristine test uses strict `<`, where the current Restunts C hint says `<=`; the other tests and in-place clamps emitted exactly under pinned MSC 5.10. See `recovery/experiments/rect-intersect-first-condition-20260923.md`.
The 172-byte `shape3d_init_shape` contribution is promoted. Its complete 69-instruction pristine CFG has no branches, calls, padding or relocations. Restunts supplied the field and assignment hypothesis, but its paint-count declaration was too wide for the pristine byte store. A local far-pointer alias emitted 190 bytes; direct argument reads matched the complete 172-byte contribution. See `recovery/experiments/shape3d-init-shape-20260923.md` and `recovery/symbol-source-audit-20260923.md`.
The blocked `rect_adjust_from_point` now has a 76-byte register-coordinate research output matching 31/33 instructions; only two BP-2 versus BP-6 store displacements remain. Moving its temp declaration before the register coordinates produced identical code. See `recovery/experiments/rect-adjust-register-20260923.md` before further source work.
The `file_get_shape2d` Restunts huge-pointer source emitted 114 bytes and an `__AHSHIFT` fixup; two explicit linear-address alternatives removed the helper but emitted 138/146 bytes. It remains raw-owned and interval-blocked pending a source/TU explanation of the pristine 95-byte inline conversion. See `recovery/experiments/file-get-shape2d-source-20260923.md`.
For `pad_id`, a long-copy source reproduces the far AX/DX loads but gives a BP loop index; a register index selects SI yet retains a two-byte frame and NOP (58 versus pristine 54 bytes). MSC 5.00 and 5.10 emitted identical code for that register-index source. No data binding or production attempt followed; see `recovery/experiments/pad-id-20260923.md`.

Refresh and validate after changes:

```text
python tools/import_restunts.py
python tools/validate.py
```

The importer needs the pinned Restunts checkout and Capstone5.0.3 (`python -m pip install --target build/python capstone==5.0.3`). Repository/source hashes must agree with `layout/references.json`. The oracle and production build do not need Restunts binaries or Capstone.

`layout/toolchain.json` pins local compiler directories and the MS-DOS Player runner. When moving machines, reconfigure paths explicitly while preserving hashes, then rerun both compiler and independent runner checks. Never copy proprietary tools into tracked source. Complete download provenance is stored under recovery; local extraction helper is `toolchain/extract_pcjs.py`.

Source promotion is serial. Parallel work may inventory/research reference bytes and toolchains, but must not mutate production ownership concurrently. Preserve user files. Current status is generated under `docs/current`; old experiments belong under `docs/history` or recovery receipts.
