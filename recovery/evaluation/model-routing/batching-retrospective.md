# Stunts batching retrospective

Scope is the eight matched Luna/high and Astra/high capsules for `copy_string`, `rect_compare_point`, `parse_shape2d_helper`, and `unknown_libname_1`. This says nothing about SimAnt. Counts are from each capsuleâ€™s `research/trials.jsonl` and `final.json`, cross-checked against `build/model-routing-pilot/run-telemetry.json`.

The matched runs contain 44 hypotheses. Forty trial results have an effective compiler identity; deduplicating within each case across both models leaves 24 identities. There are 10 repeat occurrences within a run and 6 identities produced in both model arms. Those six are matched replications, not wasted search. Effective identity includes the compiler profile and flags, so these are recipe-bound identities, not claims about unique machine-code streams. Four trials have no identity: rectangle Luna t03 lacked the optional MSC500 profile; parse Luna t02 hit a compiler error and t07 lacked the optional profile; unknown Astra t06 produced a QuickC object rejected for BAKPAT. A rejected object is not a known accepted identity.

Per case, the records are: `copy_string` 2 hypotheses, 2 identity-bearing results, 1 case-level identity, 1 cross-arm replication; `rect_compare_point` 5, 4, 3, 1; `parse_shape2d_helper` 18, 16, 8, 2; `unknown_libname_1` 19, 18, 12, 2. Within-run repeats were 0, 0, 6, and 4 respectively. Copy and rectangle both reached strict exactness in each arm; the other two did not. The repeated parse outputs are specific: Astra t01/t02/t05 shared one identity and t04/t08 another; Luna t01/t04/t09 and t03/t06 repeated those same two identities. Unknown Luna t01/t03/t05/t10/t11 shared one identity four times after its first appearance. The cross-arm overlaps include copy t01/t01, rectangle Luna t04/Astra t01, parseâ€™s two identities above, and unknown Luna t02/Astra t03 plus Luna t07/Astra t01.

Across these runs the strict helper used three compiler processes when a candidate passed through fast, staged, and canonical checks. The 50 total compiler processes consumed about 25.1 seconds of recorded trial time; the eight model launches consumed about 2,342 seconds. This gap suggests a batching opportunity, but does not measure tokens or money saved: these were not randomized batching conditions, the arms had different hypotheses and outcomes, and preparation/supervisor traffic is separate.

The 44 sequential hypotheses were not 44 candidates that could have been submitted up front. Most later moves depend on observed mismatch islands, repeated effective identities, or convergent optimization outputs; examples include parse trials Astra t05/t08 and Luna t05/t08/t09, and unknown Luna t05/t10/t11 and Astra t07/t08. A conservative pre-outcome estimate is one initial batch of 2â€“4 source hypotheses for an unresolved task. `unknown_libname_1` is the best candidate because its target code shape is compact and both runs stopped unresolved. Freeze both arms from historical `recovery/attempts/unknown_libname_1/0001`, preserve that attempt as the only negative history, and omit every later pilot result from both task packets. Keep strict profile, flags, candidate seed, evidence, and 4-hypothesis / 8-process caps identical. For the batch, archive complete sources and predictions before the first compiler call. Compare this with adaptive sequential review, where each prediction is recorded before its own call and later hypotheses may respond to earlier outcomes. The helper can spend three processes on an exact candidate; the process cap includes that verification. The task cards should not contain proposed answers or known output identities; use the existing identity set only in a supervisor-side dedup matrix.

No models or compilers were launched for this retrospective or capsule preparation. No batching roundtrip or token savings are claimed, and no conclusion is made about independent test performance.

The user approved the two additional Luna/high runs and their Stunts evidence transfer to api.openai.com. Both started from the historical 0001 seed in fresh contexts. Common inputs match; only TASK.md differs. Results belong to a separate batching cohort, not the eight-run model comparison.
