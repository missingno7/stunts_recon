# Complete callback-object LINK differential, 2026-09-24

Authority: **research-only historical LINK arithmetic**. `tools/frame_link_probe.py`
passes the unmodified archived `set_frame_callback` object (SHA-256
`fd3f9f5921964435405c6e30e51b599f3d67bf6621f0b320caaf3cab5c8e0758`)
to the pinned LINK 3.65 with `/NOD /MAP`. A separately compiled MSC 5.10
provider defines the two callback code publics and two synthetic DATA publics;
an auxiliary object supplies `__acrtused`. A pinned `__aFlmul` library member
shifts the code frame in the third case. These providers are fixtures, not
claims about historical Stunts ownership. Full maps, raw object identities,
commands, logs and EXE identities are archived in ignored
`build/frame-link-probe.json` and can be regenerated by the script.

The candidate contributes exactly 28 `UNIT_TEXT` bytes, one public at zero,
and zero other segment bytes. Its complete FIXUPP order is `offset16@24`
(`_byte_442E4`), `pointer32@15` (`_timer_reg_callback`), `base16@10` and
`loader-offset16@7` (both `_frame_callback`), then `offset16@2`
(`_word_46468`). All are zero-addend, non-relative external target-frame
records. The fixture derives every expected operand from the independent
LINK map, compares all 28 linked bytes, and checks the complete MZ relocation
list against the original fixup order. It also rechecks that LINK left all
input objects unchanged.

| Input order | Candidate load start | Callback paragraph | DGROUP load frame | Ordered MZ relocation load offsets | Full bytes equal map-derived result |
|---|---:|---:|---:|---|---|
| candidate, provider, auxiliary | 0 | 0 | 32 | 17, 10 | yes |
| provider, candidate, auxiliary | 4 | 0 | 32 | 21, 14 | yes |
| pinned library, candidate, provider, auxiliary | 52 | 3 | 80 | 69, 62 | yes |

The shifted case is material: LINK writes nonzero paragraph 3 to both the
callback `base16` field and far-call segment word, while DGROUP moves to 80.
Reversing or omitting a FIXUPP record is rejected by the complete-shape gate.
This establishes arithmetic for this *one complete object shape*; it does not
prove the original PUBDEF names, original storage owner/TU, or native strict
acceptance. No production binder mode or recipe is enabled by this report.

Independent pristine evidence pins the data addresses without assuming their
owner. Startup anchors DGROUP at `0x2B770` and clears BSS
`[0x30D3A,0x36490)`. `frame_callback` compares the byte at DS:`0x8B74`
(load `0x342E4`) at 75170 and increments the word at DS:`0xACF8`
(load `0x36468`) at 75338. The separate mapped `update_frame` reads that
word at 42205 (`8B 1E F8 AC`). The word's two bytes fit inside BSS. These
instructions corroborate address and width, while Restunts labels remain
source-name hypotheses; no original object or PUBDEF ownership is established.

Next bounded decision: specify a fail-closed binder only for this five-fixup
family, including ordered relocation obligations and the callback pointer
pair, and require independently reviewed code/data aliases before a fresh
whole-image promotion attempt. If no safe alias proof exists, retain this
fixture as a diagnostic and keep the task SUPERVISOR-blocked.

The subsequent bounded binder, independently reviewed aliases, and strict
promotion are recorded in `frame-callback-promotion-20260924.md`. This LINK
fixture remains research evidence; the later native whole-image receipt is
the acceptance authority.
