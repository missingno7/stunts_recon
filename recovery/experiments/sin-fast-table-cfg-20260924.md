# `sin_fast` table-aware CFG discriminator, 2026-09-24

**Authority:** research only; no eligibility, boundary, owner, or source promotion. The current `seg012:line8606` card locks pristine `[141022,141100)` (78 bytes, SHA-256 `af93b215c6978cf22f34e6e96798938909f5d5e4436115b445c8289e44226e1a`). The table census independently brackets `[141042,141050)` with exact source-emission bytes, no MZ relocation, and a following anchor at `141050`.

I decoded the two code slices around the table separately with pinned Capstone 16-bit mode, checked that their instruction bytes tile the slices exactly, and traversed their direct fallthrough/return flow. The ten bytes immediately before the table are `AND BX,3; SHL BX,1; JMP word ptr CS:[BX+table]`, so the reviewed path indexes four 16-bit entries. The table words resolve in the anchored segment frame to instruction starts `141050`, `141060`, `141073`, and `141085`. All 32 decoded instructions and all 70 code bytes are reachable; the 8 table bytes are excluded from instruction decoding. No flow gap appears **for the reviewed entries**.

The current adjacent `cos_fast` card (`seg012:line8655`) locks pristine `[141100,141112)`. Its direct short jump at `141109` targets `141028`, inside the `sin_fast` body before the mask and dispatch. The final byte at `141111` is recorded as raw zero emission; its historical padding ownership is not inferred. Full values and input identities are in [the machine report](sin-fast-table-cfg-20260924.json).

**Decision:** the known cross-entry is enough to reject an independent single-entry `sin_fast` contribution. Keep `sin_fast` and `cos_fast` in CFG/ownership review, even though the local table dispatch is understood. A complete ingress/public sweep and original contribution evidence are still missing. The next bounded test is to inspect independently mapped callers/other entries into this code span and determine whether a shared original contribution is supported; stop if an unresolved table-byte ingress or alternate internal entry appears. The current source representation and this local CFG do not prove historical TU/object boundaries. No strict C bytes, queue eligibility, or historical LINK acceptance changed.
