# Protected TU-context control, 2026-09-24

`tools/tu_context.py` now accepts optional `protected_neighbors` and a `protected_component` in a research-only manifest. Each protected neighbor names the exact public, the variants where it must be present, and a pristine image interval with a locked SHA-256. The tool compares its complete public-bounded literal bytes and rejects fixups for the exact-window status. The component guard compares the entire declared code contribution, complete public order/offsets, ordered fixups, and zero other segment contributions against a locked pristine interval. Every variant still archives complete OMF declarations, segment lengths, bytes, externals, local symbol records, and ordered fixups. Target body SHA and source composition remain frozen.

The updated `recovery/tu-context/rectangle-order.json` provides a positive control under pinned MSC 5.1. The target `_rect_is_overlapping` stays 62 literal bytes and fixup-free in isolated, predecessor, and successor variants. `_rect_is_inside` is independently promoted and locked at pristine load `[92572, 92624)` (52 bytes); its public-bounded window is exactly 52 bytes with no fixups in both variants that include it. The successor variant reproduces the reviewed 114-byte pair at `[92510, 92624)`, with publics `_rect_is_overlapping@0` and `_rect_is_inside@62`, zero fixups, and no other nonempty segments. The predecessor changes public order and target offset by 52 bytes while preserving both literal function windows; it is deliberately not required to match the historical-order pair. The new run archived complete sources, objects, and results under `build/private/tu-context/cd86c9468e6e/`.

These guards are diagnostic, not production acceptance. A public boundary may still hide private code; this fixture is exact because the protected windows and complete pair extent are separately locked. The experiment does not prove that the original game linked these source functions from one historical object. Build topology still records zero proven same-object pairs or boundaries.

The next mapping family is also bounded: 15 of 33 current partial tasks contain 18 `dw offset` directives. The source includes both jump tables and data-offset tables. Current cards lack complete exact table-span proof, so no task was reopened. A future parser must require uniquely resolved same-segment target coordinates and a complete byte-for-byte table span bracketed by independent anchors; code/data status stays separate.
