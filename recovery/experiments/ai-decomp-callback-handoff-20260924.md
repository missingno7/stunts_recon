# ai_decomp handoff: callback binding checkpoint, 2026-09-24

**Native strict gain:** one 28-byte C function, `set_frame_callback`.
Fresh pinned MSC 5.10 emitted the same complete object SHA-256 as the
research candidate, the bounded binder matched all pristine bytes and ordered
MZ relocations, and grinder receipts `0001` FAST / `0002` PROMOTED passed
fresh whole-image acceptance. Final validation: 178 tests, 26 strict game-C
functions / 1,118 bytes, `HYBRID_EXACT`; CHEAP 0 / MEDIUM 7 /
SUPERVISOR 576 (previously 577). This is an accepted hybrid replacement,
not a demonstrated workflow speedup or historical TU/data-owner proof.

**What changed the decision:** the archived object already had a complete
28-byte contribution with all five ordered fixups. Three historical LINK
placements, including nonzero code paragraph, reproduced map-derived full
bytes and relocation order. Separate pristine accesses established the
callback code target and both BSS addresses. A dedicated
`external-frame-callback-v1` mode now fails closed on object/segment/public/
fixup/symbol/relocation changes. `frame-link-differential-20260924.md` and
`frame-callback-promotion-20260924.md` carry the fixture and native receipts.

**Still ruled out/underconstrained:** the two archived 24-byte two-public
`file_load_shape2d_nofatal` objects remain contradictory or 686 bytes short
of the anchored interval; shared-base remains a research-only diagnostic.
The 33-byte `mmgr_get_chunk_size` private-DATA string still differs from
the 46-byte pristine message, with only one direct reference and no original
owner. No placement-only retry is warranted. After this promotion the fresh
archived SUPERVISOR OMF census has **zero** complete nonfixup-shape candidates;
another abstract binder expansion has no currently demonstrated strict target.

**Next bounded test:** take the archived `audio_add_driver_timer`
(`load_16bae`) 52-byte candidate against its 39-byte pristine interval.
Freeze its target source body and vary only historically plausible same-TU
callback declaration/definition and order. Measure the target window, full
contribution, publics and complete fixups. If the unchanged target contracts
or changes call/pointer lowering, route this family to TU-context recovery;
if it remains 52 bytes, retain the source-shape/ABI blocker. Do not extend
production pointer binding from this near match, and do not promote a context
diagnostic without fresh native whole-image acceptance.
