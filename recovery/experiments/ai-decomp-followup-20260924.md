# ai_decomp follow-up: timer context and shared-entry CFG, 2026-09-24

**Native strict gain:** zero this follow-up. Fresh independent validation passed 179 tests and all 26 active C contributions; `HYBRID_EXACT`, 1,118 strict C bytes. The refreshed queue stayed CHEAP 0 / MEDIUM 7 / SUPERVISOR 576. The archived SUPERVISOR OMF census still has zero complete nonfixup-shape objects ready for another binder promotion.

**Ruled out:** freezing the `audio_add_driver_timer` body and moving a public far callback definition before/after it did not change the 52-byte target or its semantic fixups. The 39-byte pristine interval remains unmatched. Raw OMF index renumbering had created a false two-class TU-context result; the comparator now preserves raw fixups but groups by resolved meaning where names are known. The corrected result is one target-output class. This is a diagnostic correction, not a speedup or TU proof. See `audio-timer-tu-context-20260924.md` and the archived complete objects.

**New mapping constraint:** `sin_fast` has a verified 8-byte four-entry table and 70 reachable code bytes under its reviewed dispatch. The adjacent `cos_fast` jumps into that body at `141028`. The local CFG has no gap for reviewed entries, but the cross-entry blocks independent single-entry ownership. See `sin-fast-table-cfg-20260924.md` and its machine report. No boundary, owner, or queue promotion was made.

**Still underconstrained:** the contradictory and 686-byte-incomplete two-public file objects remain excluded from placement-only retries. The private-DATA message remains 33 candidate bytes against 46 pristine bytes under the proposed association, with only one independent reference. Shared-base and table CFG are research-only; neither substitutes for complete contribution, ordered fixups, historical LINK, or native whole-image acceptance.

**Next bounded test:** seek independent ingress/public evidence for a shared `sin_fast`/`cos_fast` contribution, including possible entries into table bytes, before source work. If the shared boundary remains unknown, move to another mapped source-shape candidate with a complete oracle-facing discriminator; the current census gives no immediate binder-only target. Do not repeat callback stub-order variants for `audio_add_driver_timer`.
