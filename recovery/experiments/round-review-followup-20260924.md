# Round-review follow-up, 2026-09-24

Authority: research only. The shared-base census remains a placement diagnostic;
none of the work below changes production binding, task eligibility, or the
strict whole-image oracle. The two archived 24-byte two-public wrapper objects
remain outside placement-only retry lanes.

## Complete contribution gate and bounded alternative

The archived `file_load_shape2d_nofatal` local-stub objects are not sufficiently
complete candidates. Trial 001's two public equations disagree by 24 bytes.
Trial 002 places both publics consistently at load base 174570, but its entire
`UNIT_TEXT` is 24 bytes while the anchored wrapper plus callee interval is
174570..175280 (710 bytes): 686 bytes are absent. The observed `+5` local call
geometry cannot discharge this extent, fixup, or historical linker obligation.
There is no independently evidenced complete multi-public replacement in the
current archive. A complete 692-byte callee is **not** assumed to be a cheap
diagnostic prerequisite; a future candidate needs independent contribution
extent/public/fixup evidence before retry.

The separately archived `set_frame_callback` object is an eligible *research*
alternative. `python tools/probe_frame_contribution.py` reproducibly checks
the full 28-byte code contribution and zero DATA/CONST/BSS contribution,
the sole ordered public `_set_frame_callback@0`, all five ordered OMF fixups,
16/16 nonfixup bytes, each pristine operand, and original MZ relocation order
at load offsets 75115 then 75108. Independent pristine instructions outside
the target corroborate the callback pointer, timer call, and both data
addresses. The frozen output is
`recovery/experiments/set-frame-contribution-20260924.json` with authority
`RESEARCH_ONLY_COMPLETE_SHAPE_AND_OPERAND_CHECK`.

This experiment **changes the next recovery decision** for that candidate:
local source spelling is no longer the immediate test. The next bounded test
is a historical LINK differential fixture for this complete five-fixup mode
(including callback offset/base pair and the exact ordered relocation list),
with a negative control for reordered or omitted fixups. Independently review
the two BSS data addresses and ownership before any production binder recipe;
then require fresh native whole-image acceptance. The diagnostic does not
measure a recovery speedup and does not prove historical object/TU membership.

## Private DATA branch

The original message at load address 196339 is 46 bytes through its NUL:
`memory manager - BLOCK NOT FOUND at SEG= %x\r\n\0`. The following named
`aResizememoryCa` string begins immediately after it in the reference data
listing. Both archived `mmgr_get_chunk_size` objects contain only a 33-byte
candidate `_DATA` string: the first 32 bytes agree, then candidate NUL meets
pristine space. Thus the message extent/content is known, but the candidate
does not supply it.

The source listing has one direct `aMemoryManagerB` use, the `MOV AX,4783h`
inside mapped `mmgr_free` at 136347. Searching the pristine load image for
the little-endian value `83 47` finds that immediate at 136348 and one other
occurrence at 169625. The latter is the opcode/operand prefix of
`83 47 08 01` in `sub_3968A`, not a second message-address reference.
No second independent direct reference has been established. This search is
not a proof that no indirect reference exists. Private-DATA placement remains
single-anchor, and historical ownership remains unknown; no placement repair
or source promotion is proposed.

## Accounting and strict state

The wrapper summary previously stopped after its first two compiler receipts.
Two later same-TU stub trials have separate retained compiler invocations and
objects. One adapter call failed with `FileExistsError` before compiler launch.
The corrected ledger records four compiler processes, four distinct effective
code identities (20, 18, 24, 24 bytes), zero repeats, and one prelaunch
failure. Its former two-class count was undefined; it may have meant two broad
experiment families but contradicted the four stored effective identities.
All original trial receipts and hashes remain in place.

`python tools/validate.py` passed 173 tests, independently compiled and bound
all 25 active game-C functions, verified the exact whole-image build, and
refreshed the queue to CHEAP 0 / MEDIUM 8 / SUPERVISOR 576. Native strict gain
this follow-up: **zero functions and zero bytes**. No new production binder
mode, historical TU boundary, or private-data owner was claimed.

Next private-DATA test: seek another independently mapped instruction or an
original object/data contribution that establishes the 46-byte message owner.
If neither exists, keep this branch parked and continue with the complete
`set_frame_callback` five-fixup LINK fixture rather than another placement-only
wrapper or private-string retry.
