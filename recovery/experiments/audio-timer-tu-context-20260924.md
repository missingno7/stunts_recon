# Timer TU-context discriminator, 2026-09-24

**Authority:** research only. The fresh pinned MSC 5.10 `/AM /O /Gs` experiment froze the `audio_add_driver_timer` target body at SHA-256 `d7ed9264bb9520d9bb13eb5b25a7f6b46dc40a5b90bcd065cfaefcf9e522c91e`. The archived 52-byte candidate and current 39-byte pristine interval motivated the test. Three conditions varied only callback declarations/definition and order: external, same-TU public successor (mapped seg007 order), and same-TU public predecessor. The defined callback was a 2-byte stub, so the experiment cannot test complete original contribution or prove original TU membership.

All three emitted the same 52-byte target window, SHA-256 `d972b5823727006dc1562be76bbc111260e031cb6610c32d11211388b37a7877`; none matches the 39-byte oracle. The external object has `UNIT_TEXT=52`, one public at +0, and ordered target fixups `pointer32@44` to `_timer_reg_callback`, `base16@39` and `loader-offset16@36` to `_audio_driver_timer`. The successor object has `UNIT_TEXT=54`, publics callback@52 / target@0, and the same normalized target fixups. The predecessor has `UNIT_TEXT=54`, publics callback@0 / target@2; its full-object fixup offsets shift by +2 but the target-relative offsets are identical. `_DATA`, `CONST`, and `_BSS` remain zero in all three. Public/list/extent differences are retained in the complete OMF receipts.

Raw OMF symbol indexes changed between variants, which initially split identical target code/fixup semantics into two apparent output classes. `tu_context.py` now keeps raw ordered records and separately compares resolved fixups without numeric `frame_index`/`target_index`. The corrected report has **one semantic target-output class**, while full-object topology remains different. The first three compiler runs and three repeated runs for corrected reporting are separate; the latter did not add a new experimental condition. This fixes a reporting confound, not a recovery speedup.

**Decision:** the tested same-TU callback visibility/order does not contract or otherwise change this target. Retain `load_16bae` in source-shape/ABI research; its stack-local cursor and prologue differ from the pristine BX loop. Do not run more stub-order variants or promote a binder from this 52-byte object. Strict gain is zero; no queue eligibility changes. Raw results: `build/private/tu-context/4127a0ae16b2/results.json` and corrected `build/private/tu-context/d02d1220eba9/results.json`. The tracked manifest is `recovery/tu-context/audio-timer-order.json`.

**Next bounded recovery test:** the current complete-candidate census has zero unpromoted SUPERVISOR objects with complete nonfixup shape, so another binder pass lacks a concrete acceptance target. A read-only audit of current `sin_fast` (`seg012:line8606`) found an independently bracketed exact 8-byte four-entry table in its 78-byte source-emission interval `[141022,141100)`. All four table words target code inside the interval, while the adjacent `cos_fast` jumps back into its body. Test a table-aware CFG traversal that includes this shared-body ingress and searches the pristine segment for other entries or table-byte ingresses; fail if any target, fallthrough, or incoming edge is unresolved. This can refine mapping/CFG ownership only, not make `sin_fast` an independent single-entry C task. The exact table receipt is in `recovery/table-offset-census.json`, and the current card is `recovery/cards/seg012_line8606.json`. Keep the two short multi-public file objects and the incomplete private-DATA message out of placement-only retries. A new full contribution or second independent DATA reference is still required for those branches.

**Validation:** `python tools/validate.py` passed 179 tests and fresh independent DOSBox-X checks for all 26 active C functions; `HYBRID_EXACT`, 1,118 strict C bytes. Reclassification kept CHEAP 0 / MEDIUM 7 / SUPERVISOR 576.
