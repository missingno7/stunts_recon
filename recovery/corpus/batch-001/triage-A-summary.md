# Triage A summary

- Scope: 12 selected SUPERVISOR cases plus controls 0–2; read-only; zero attempts/reopens. All 12 `context.py` requests failed twice on `Context index/card changed; refresh queue`; findings use the frozen baseline selection and selected card/archive evidence.
- Unsupported capability is distinct from missing evidence: `file_load_shape2d_nofatal` needs an unreviewed near-call binding; `mmgr_get_chunk_size` also needs an unsupported cross-contribution jump mode; `criterr_exithandler` uses unsupported CS-relative data. By contrast, `init_unknown` has six unmapped DGROUP symbols where the reviewed offset16 mode may suffice after mapping.
- Source/compiler questions remain evidence-limited: `update_rpm_from_speed` has direct 16×16 MUL/high-word target behavior but source types are absent; `sub_307D2` is a nibble-indexed byte lookup with compiler/TU/padding provenance missing; `nopsub_19DE8` is a sign function but original compiler family is unconfirmed. SS access and BIOS/DOS interrupt behavior require source/semantic review before calling them tool limitations.
- Higher-level signal: `parse_shape2d_helper` target computes `(segment << 4) + offset`; all three archived algebraic spellings produced the same object (`6465f75d…`, 26-byte text vs 32-byte target), with attempts 2 and 3 explicitly deduplicated against earlier output. This supports changing analysis level, not another syntax retry; the causal source shape remains unknown and budget is zero.
- `is_facing_camera` already has reviewed far-call binding and ABI/anchor evidence, while its known 194/200-byte local-slot mismatch remains; the specified paired-long-delta/byte-result evidence was not found in this triage. No new reopen basis was established.
- `rect_union` remains nonexecuting: its selected 122-byte card has no decoded rows, and its referenced original assembly path is absent from this workspace; the stale context packet could not supply a raw artifact link.
- All three controls preserve unchanged: current `src/*.c` identities equal their promotion fast-source receipts; records show promoted exact payloads and matching staged/canonical full-image hashes; baseline `validation.json` says PASS for independent compiler parity over 10 active functions and each control’s parity result is `exact: true`. No control edits or compiles proposed.

Detailed per-case evidence, fact learned, missing/optional evidence, capability sufficiency, next action, authorization state, source/object hashes, and parity receipts are in `triage-A.jsonl`.
