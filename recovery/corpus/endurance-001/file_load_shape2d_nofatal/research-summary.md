# file_load_shape2d_nofatal endurance result

**Status: unresolved, budget-censored.** The first phase ran two compiler processes under `msc510-medium` with `/AM /O /Gs`. A later same-TU extension ran two more, for **four actual compiler launches**. One intervening research-adapter call failed before launch because its work-directory name already existed; it consumed no compiler process. The frozen target is 18 bytes, SHA-256 `8c0aee23fb3b48de8f827aa81776bc9619edb46ef9a326b248d548968486d7cf`.

Trial 1 isolated the far-return wrapper and declared the callee external with its far-pointer return. MSC emitted 20 code bytes and a pointer32 external fixup to `_file_load_shape2d` (effective identity `d6eb7dceb5889480bd893a7f8b995106caf4f2cd7abb35a77c0af9be1319ff2a`). Prologue, argument pushes, cleanup, and return were anchored; call lowering was a far `9A` instead of target `PUSH CS; CALL near`, plus the object had an alignment NOP. Extent and bytes failed.

Trial 2 declared the external callee near. Code length became 18 bytes, but bytes still differed (effective identity `f33bac3690c5f1ecec06bcf61768af2a3152e273ba4faec2713c515fe7fc40d1`). The call became `CALL rel16` with a self-relative `offset16` fixup to `_file_load_shape2d`; there was no `PUSH CS`, and the code ended with a NOP. This establishes that the explicit near declaration changes call mode and that the object carries a same-segment relocation requirement. It does not produce the target's far-call stack setup or resolved +5 displacement.

The target assembly shows `PUSH CS; CALL near` to the immediately following `file_load_shape2d` implementation in the same segment; historical TU membership remains unproved. Two later same-TU stub candidates **were compiled** and retained under `trials/001` and `trials/002`. Their 24-byte objects differ in public placement: wrapper at offset 6 versus wrapper at offset 0 and callee at offset 18. The first contradicts the two pristine public-address equations. The second has a consistent base, but its complete code contribution is 24 bytes against a 710-byte anchored interval, omitting 686 bytes. The `+5` call geometry is diagnostic only; neither object has complete contribution or historical linker acceptance. The earlier `candidate-unrun-same-tu.c` label records the initial phase and is superseded by those compiled trials.

This is useful narrowing of a workflow/TU gate, not evidence that Luna cannot solve the source task. Strict wrapper extent matched once in the isolated phase, but complete bytes did not; the local-definition phase lacks the remaining contribution. No exact or acceptance claim is made. The ledger now records **four distinct effective-code identities and zero repeats**. Its previous `unique_output_classes: 2` was undefined and contradicted the four stored identities; if it meant two broad experiment families, it was not a deduplicated-output count. The per-trial receipts and hashes are unchanged.

Per-trial artifacts include source and precompile prediction/falsifier, compiler receipt/log, full object/code and OMF declaration/fixup details, target/code bytes, strict comparison, and compact/full frozen-engine diagnostics. Engine SHA-256: `a5df723b30e4581e53434d32dea82798c625c34e84bf55330c56a7f81c23ccc3`. Token usage by model/cache category is unavailable.
