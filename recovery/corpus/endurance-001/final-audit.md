# Final audit: endurance-001

The frozen cohort records reconcile: 10 initial compiler processes plus 31 endurance processes equals 41/50. Endurance outputs comprise 21 new effective identities and 8 repeats; two additional compiler processes were syntax-rejected, yielding 31 total. The ledger has 12 rows (10 cases and two zero-compile controls). Case-process totals and output classes match the summary; there were also two separate precompiler/harness errors. No endurance candidate was promoted.

The 34-case-slot allocation leaves three unused case slots, plus four reserved and two safety slots: nine of the global 50 remain unused. This is a capped purposeful sample, not global exhaustion. Per-case quota stops and convergence stops are workflow/budget decisions. The B4 cases have compiler-emitted objects; where the strict production reader rejected B4, isolated generic parsing and diagnostics recovered evidence. In RPM, H2 is a complete 170-byte object with B4/LEXTDEF and a local helper fixup, not an unresolved parser observation. The production-reader gate remains. The two named-segment syntax rejections establish a pinned MSC 5.10 capability boundary.

Of eight output repeats, five are same-source MSC 5.0/5.10 controls and therefore informative about profile sensitivity. Three are convergent source-form repeats: the register-qualified far formal in `file_get_res_shape_count`, signed-char result form in `is_facing_camera`, and repeated expression form in `mmgr_get_chunk_size` each reproduced a known effective output. These tested forms were ineffective; they do not establish model exhaustion or rule out other source/context hypotheses.

Remaining data-quality limits: compiler-token usage is absent from endurance ledger rows; effective-identity evidence is distributed among case receipts; original C/TU/caller or mapped-data evidence remains incomplete for several cases. Thus counts are auditable, but the purposeful sample cannot estimate solvability or comparative model efficiency. Stops after three failures must be labeled workflow-gated unless evidence independently demonstrates model exhaustion.
