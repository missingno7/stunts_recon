# mmgr_get_chunk_size endurance result

Research is unresolved and budget-censored after three compiler API invocations. The SUPERVISOR card lists two workflow constraints: external/indirect jump/TU review and unmapped DGROUP addresses 0x4ba0/0x4ba2. Verified target is 38 bytes (`4981ce0e781e5b9d402147f8588255f57c61b1f6c8cade357ce919378d166f89`).

The ordinary-C object exposed concrete linkage requirements. With `struct MEMCHUNK` fields at offsets 0x0c (`ressize`) and 0x0e (`resofs`), and external near globals `resptr1` and `resptr2`, MSC 5.1 emits OMF offset16 fixups to `_resptr1` and `_resptr2`, a DGROUP/_DATA offset16 fixup for the far-error string, and a pointer32 far-call fixup to `_fatal_error`. These names agree with the Restunts declarations; `mmgr.c` defines both globals from `resources`. This identifies symbol-level mapping work but does not establish their runtime DGROUP offsets or satisfy binding.

Trial 01 used a union view for the far pointer and emitted 84 code bytes. Trial 02 switched to a long-pointer cast; output changed to 72 bytes and demonstrated a distinct spill/cast lowering, while the function remains far under the pinned `/AM` medium model. Trial 03 restored the union view with explicit `far` on the function and reproduced trial 01 exactly: same complete object hash `93b66a5eeb0541ef068323a58c83d7a45409d31f36473a79861618bf7178d4e5`, code hash `b8985dc43682434aa8024c141898e5108777122a8bfc143cc9ba599f54b1a798`, effective identity `cc0141218b8351e4f9c7115cd33fcc3f122c5661968c6c8e733ffc0f38a59775`. This is one repeated source-shape/output class, not new progress.

Every candidate remained larger than the 38-byte target (84, 72, 84) and retained fixups; none matched strict extent or bytes. All three diagnostic reports preserve one short prologue anchor, then a single mismatch island. Target loads `resptr2`, checks `resptr1`, scans 0x12-byte records, and on not-found near-jumps to `loc_31498` in adjacent `mmgr_free` failure handling. Isolated C instead emits a far `_fatal_error` call. That cross-function shared tail is a TU/source-structure or assembly-origin question beyond local declarations. The full diagnostics also show the argument representation/spill difference; no diagnostic normalization is acceptance evidence.

The case demonstrates both a workflow gate and remaining source/codegen uncertainty. Luna mapped exact symbol names, offset16 relocations, the required error call mode, record stride/field offsets, and a repeated effective object class. A complete address map, linker/TU evidence for the `loc_31498` continuation, and proof that the pinned profile can express the shared tail are still needed. No exact candidate, binding, promotion, or Luna failure is claimed; it is budget-censored.

Compact and full diagnostics are archived per trial with engine SHA-256 `a5df723b30e4581e53434d32dea82798c625c34e84bf55330c56a7f81c23ccc3`. Token usage by model/cache category is unavailable in the execution surface.
