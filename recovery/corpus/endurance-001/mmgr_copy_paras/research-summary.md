# mmgr_copy_paras endurance result

Research is unresolved after three API invocations, of which two reached the compiler; one actual compiler-process slot remains unused. The original short-budget task stopped before compilation because `#include <dos.h>` is outside the compiler API preprocessor closure. An isolated ordinary-C candidate removed that include and expressed far pointers directly.

The target is the verified 72-byte contribution (`fd89188541b8f5e7b5b4c49930b436c762fb41875db0281e25841e4bd555fc50`). Target behavior includes `CLD`, `REP MOVSW`, `DS`/`ES` advances of 0x1000, and far return. Trial 02 compiled 108 bytes; trial 03 tested an unsigned chunk-threshold formulation and compiled 114 bytes. Both are complete fixup-free objects, but neither matches target extent or bytes. The machine mismatch persists across the whole function; diagnostics retain the initial `push bp; mov bp,sp` anchor only. The ordinary C scalar word loop did not lower to `REP MOVSW`. Trial 03 did not establish that source idiom as the only possible explanation; broader compiler/TU evidence is needed.

Quota accounting: invocation 1 failed inside the research harness before invoking the compiler process (not charged against the 3-process hypothesis cap); invocation 2 compiled source hash `651b290a1d32669167f69fd5dec19076e219eafb3aa4b52045b959d99ea93a4a` into a 108-byte object (effective identity `9144a7cebcc5b7cd3619da6a64bfadc433258104119f28308715531ca217687b`); invocation 3 compiled source hash `4f07a748a322e69f56820c22022bcd54938f6288beb4bba8962dec320f303d01` into a 114-byte object (effective identity `a653d577abd97700be6fe5ea59fe7ad96ae57bd25da7df0b0b065323af929155`). Two compiler processes ran and produced two unique effective output classes. No source was repeated for a new result. Exact argv for successful processes is in each receipt.

Established: the include closure was a workflow gate that could be bypassed for isolated study. The chosen C forms do not reproduce the target's string-copy sequence or compact segment/chunk loop. Unresolved possibilities include a different C/API-level runtime routine, special source/tool profile context, or assembly-origin/mixed source. The API prohibits inline assembly and raw emission, and this case did not test a different pinned profile or TU. No Luna failure or strict acceptance claim is made. The case was stopped before its compiler-process cap because no further evidence-supported ordinary-C hypothesis remained; one slot is unused.

Compact/full diagnoses are archived per trial under this directory and identify engine SHA-256 `a5df723b30e4581e53434d32dea82798c625c34e84bf55330c56a7f81c23ccc3`. Token usage by model/cache category is unavailable in the execution surface.

